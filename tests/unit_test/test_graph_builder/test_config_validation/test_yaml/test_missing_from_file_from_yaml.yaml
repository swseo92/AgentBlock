nodes:
  - name: sub_pipeline
    type: from_yaml
    # from_file 혹은 graph 필드를 아예 적지 않음
    # => "from_yaml node must have 'from_file' or 'graph'." 에러

edges:
  - from: START
    to: sub_pipeline
  - from: sub_pipeline
    to: END
